name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov
        # Install torch separately with pip's timeout extension to handle large download
        pip install --timeout=600 "torch>=2.0.1,<2.1.0"
        # Install critical dependencies explicitly 
        pip install "tiktoken==0.8.0" regex requests
        # Install the package without dependencies (since we installed critical ones already)
        pip install -e ".[dev]" --no-deps
        # Now install remaining dependencies
        pip install -e ".[dev]"
        
    - name: List installed packages
      run: |
        pip list
        
    - name: Run tests
      run: |
        pytest --cov=assert_llm_tools
        
    - name: Generate coverage report
      run: |
        pytest --cov=assert_llm_tools --cov-report=xml
        
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        fail_ci_if_error: false